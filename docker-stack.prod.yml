services:
  web:
    deploy:
      replicas: 1
      update_config:
        order: start-first
        failure_action: rollback
        delay: 5s
      rollback_config:
        order: stop-first

  tunnel:
    deploy:
      replicas: 1
      update_config:
        order: stop-first
        failure_action: rollback
